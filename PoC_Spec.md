# Kamon Generator PoC仕様

## 1. 目的
このPoCの目的は、黄金比・白銀比を用いた家紋生成が、ユーザー操作込みで安定して「それらしい形」を作れるかを検証すること。

## 2. 実装する機能
本PoCでは以下の5機能を対象とする。

1. 比率モード切り替え
2. 生成エンジンの型選択
3. ユーザー入力パラメータ調整
4. 制約付き生成（破綻抑制）
5. 比較ビュー（黄金比 vs 白銀比）

以下は実装しない。

- 品質メーター（スコア表示）
- URL共有
- エクスポート
- ランダム生成
- リセット

## 3. 機能仕様

### 3.1 比率モード切り替え
- モード: 黄金比 / 白銀比 / 1:1（対照用）
- 期待動作:
  - モード切り替え時、現在の他パラメータを維持したまま再描画する。
  - 見た目差分が視認できること。
- 初期値: 白銀比

### 3.2 生成エンジンの型選択
- 型:
  - 放射型（菊系）
  - 格子交差型（七宝・麻の葉寄り）
  - 同心反復型（輪重ね）
- 期待動作:
  - 型の変更で幾何構造が明確に変わること。
  - どの型でも比率モードが適用されること。
- 初期値: 放射型

### 3.3 ユーザー入力パラメータ調整
- パラメータ:
  - 分割数 n（6〜24, 整数）
  - 反復段数 k（2〜12, 整数）
  - 回転ステップ角 θ（5〜45, 整数）
  - 比率適用強度 r（0〜100, 整数%）
  - 線太さ w（1〜12, 整数）
  - 塗りモード（線画 / ベタ）
- UI:
  - スライダー + 数値入力
  - 値変更時にリアルタイムで再描画
- 初期値（提案）:
  - n=12, k=5, θ=15, r=70, w=2, 塗り=線画

### 3.4 制約付き生成（破綻抑制）
- 制約:
  - 回転対称性を維持
  - 線密度の上下限を制御
  - 最小線間隔を確保
  - 描画領域外への過度なはみ出しを抑制
- 期待動作:
  - 極端な入力でも描画崩壊しない。
  - 無効値は最寄り有効値へ丸める。
- バリデーション方針:
  - UIで入力範囲を制限
  - 生成直前に最終チェック

### 3.5 比較ビュー（黄金比 vs 白銀比）
- 表示:
  - 同一パラメータで左右2カラム表示
  - 左: 黄金比、右: 白銀比（固定）
- 操作:
  - 比較モードON/OFF
  - ON時は共通パラメータを同時反映
- 期待動作:
  - 比率差を一目で比較できる。

## 4. 非機能要件
- フロントエンドのみで動作（サーバー不要）
- 描画はSVGベース
- 最新Chromeで正常動作

## 5. 受け入れ条件
以下を満たしたらPoC成立とする。

1. 5機能が実装され、画面上で一連操作できる。
2. 比率切り替えで図形の差分が視認できる。
3. ユーザー入力で複数パターンを安定生成できる。
4. 破綻しやすい入力でも、制約により表示が維持される。
5. 比較ビューで同条件の黄金比/白銀比を並べて確認できる。

## 6. 実装優先順
1. 描画基盤（SVG）
2. 型選択 + 比率モード
3. パラメータUI + リアルタイム反映
4. 制約付き生成
5. 比較ビュー

## 7. 想定リスクと対応
- リスク: パラメータ同士の干渉で破綻
  - 対応: 範囲制限、丸め、フェイルセーフ描画
- リスク: 比率差が分かりづらい
  - 対応: 比較ビュー固定レイアウト
- リスク: 操作時の描画負荷
  - 対応: 計算と描画処理を分離し、再描画対象を最小化

