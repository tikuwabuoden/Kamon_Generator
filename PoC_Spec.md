# Ratio Emblem Generator PoC仕様

## 1. 目的
このPoCの目的は、黄金比と白銀比を生成ルールへ組み込むことで、
同一テンプレートからでも異なる印象の紋章を安定生成できるかを検証すること。

特に以下を確認する。

- 黄金比を用いた場合、洋風・装飾的・動きのある印象が出るか
- 白銀比を用いた場合、和風・静的・整然とした印象が出るか
- ユーザーが比較表示によって、その差を視覚的に認識できるか


## 2. 実装する機能
本PoCでは以下の5機能を対象とする。

1. 比率モード切り替え
2. テンプレート選択
3. ユーザー入力パラメータ調整
4. 制約付き生成
5. 比較ビュー

以下は実装しない。

- 品質メーター（スコア表示）
- URL共有
- エクスポート
- ランダム生成
- リセット

## 3. 機能仕様

### 3.1 比率モード切り替え
- モード:
  - 黄金比
  - 白銀比
  - 1:1（基準形）

- 期待動作:
  - モード切り替え時、他パラメータを維持したまま再描画する
  - テンプレートごとに比率の違いが見た目へ反映される
  - 黄金比では相対的に洋風・装飾的な印象が強まり、白銀比では相対的に和風・整然とした印象が強まる

- 初期値:
  - 白銀比

### 3.2 テンプレート選択
- テンプレート:
  - Emblem
    - 花弁、弧、ロゼット、星形などを含む装飾的な構成
    - 黄金比で印象差が出やすい
  - Mon
    - 同心円、直線分割、反復、余白重視の構成
    - 白銀比で印象差が出やすい

- 期待動作:
  - テンプレート変更で幾何構造が明確に変わる
  - どのテンプレートでも比率モードが適用される
  - 各テンプレート内で、比率が印象形成に効く

- 初期値:
  - Mon

### 3.3 ユーザー入力パラメータ調整
- 共通パラメータ:
  - 分割数 n（6〜24, 整数）
  - 反復段数 k（2〜12, 整数）
  - 回転ステップ角 θ（0〜45, 整数）
  - 比率適用強度 r（0〜100, 整数%）
  - 線太さ w（1〜12, 整数）
  - 塗りモード（線画 / ベタ）

- テンプレート依存パラメータ:
  - Emblem:
    - 花弁の伸長率
    - 内外半径差
    - 装飾密度
  - Mon:
    - 余白比率
    - 線間隔
    - 中心比率

- UI:
  - スライダー中心
  - 値変更時にリアルタイムで再描画
  - 現在の比率モードとテンプレートを常時表示

- 初期値（提案）:
  - n=12
  - k=5
  - θ=0
  - r=70
  - w=2
  - 塗り=線画

### 3.4 制約付き生成
- 制約:
  - 回転対称性を維持する
  - 線密度の上下限を制御する
  - 最小線間隔を確保する
  - 描画領域外への過度なはみ出しを抑制する
  - 極端なパラメータでもテンプレートの印象を保つ

- 期待動作:
  - 極端な入力でも描画崩壊しない
  - 無効値は最寄り有効値へ丸める
  - 比率適用時もテンプレート構造が破綻しない

- バリデーション方針:
  - UIで入力範囲を制限する
  - 生成直前に最終チェックを行う
  - 必要に応じて描画用の補正値へ変換する

### 3.5 比較ビュー
- 表示:
  - 同一テンプレート・同一パラメータで左右2カラム表示
  - 左: 黄金比
  - 右: 白銀比
  - 必要に応じて 1:1 を基準表示として追加検討

- 操作:
  - 比較モード ON/OFF
  - ON時は共通パラメータを同時反映
  - テンプレートも左右で共通とする

- 期待動作:
  - 比率差を一目で比較できる
  - ユーザーが「洋風寄り / 和風寄り」の印象差を観察できる

## 4. 非機能要件
- フロントエンドのみで動作（サーバー不要）
- 描画はSVGベース
- 最新Chromeで正常動作
- 操作に対してリアルタイムに近い応答を返す
- 同一パラメータでは再現性のある描画結果を返す

## 5. 受け入れ条件
以下を満たしたらPoC成立とする。

1. 比率モード、テンプレート選択、パラメータ調整、制約付き生成、比較ビューの5機能が一連操作できる
2. 同一テンプレートにおいて黄金比版と白銀比版の見た目差が視認できる
3. 黄金比版に洋風・装飾的な傾向、白銀比版に和風・整然とした傾向が一定程度現れる
4. ユーザー入力によって複数パターンを安定生成できる
5. 極端な入力でも描画破綻が起きにくい

## 6. 実装優先順
1. 単一テンプレートの描画基盤（SVG）
2. 比率モード切り替え
3. 比較ビュー
4. 2種類目のテンプレート追加
5. パラメータUIの拡張
6. 制約付き生成の強化

## 7. 想定リスクと対応
- リスク: 黄金比と白銀比の差が見た目に出ない
  - 対応: 比率を印象に直結する寸法へ集中して適用する

- リスク: ただの幾何学模様に見えてしまう
  - 対応: テンプレートごとに印象文法を定義し、装飾性と余白設計を意識する

- リスク: パラメータ同士の干渉で破綻する
  - 対応: 範囲制限、丸め、フェイルセーフ描画を入れる

- リスク: ユーザーが印象差を読み取れない
  - 対応: 比較ビューを前提とし、必要なら補助ガイド表示を追加する
